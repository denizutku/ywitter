<%= form_with(model: yweet) do |form| %>
  <% if yweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(yweet.errors.count, "error") %> prohibited this yweet from being saved:</h2>

      <ul>
        <% yweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :yweet %>
    <%= form.text_field :yweet %>
  </div>

  <div class="field">
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
