<nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">

                    <!-- Sidebar Toggle (Topbar) -->
                    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
                        <i class="fa fa-bars"></i>
                    </button>

                    <!-- Topbar Search -->
                    <%= form_tag(search_path, :method => "get", class: "d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search") do %>
                        <div class="input-group">
                            <%= search_field_tag :search, params[:search], placeholder: "Search in Ywitter", class: "form-control bg-light border-0 small" %>
                                <div class="input-group-append">
                                <%= button_tag "", class: "btn btn-primary", :name => nil %>
                                </div>
                        </div>
                    <% end %>
                </nav>